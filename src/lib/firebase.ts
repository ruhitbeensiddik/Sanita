// ============================================================
// Firebase integration is DISABLED.
// The app runs in local-only mode using localStorage.
// To re-enable Firebase, restore the original firebase.ts
// and update the store files to use Firestore/Auth again.
// ============================================================

// Stub exports to prevent import errors if anything references this file.
export const auth = null
export const db = null
export default null
